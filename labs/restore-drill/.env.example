# Optional local overrides. Defaults are already set in docker-compose.yml.
# Keep this lab tiny: do not change images unless they already exist locally.

COMPOSE_PROJECT_NAME=restore-drill
POSTGRES_IMAGE=postgres:16-alpine
CLICKHOUSE_IMAGE=clickhouse/clickhouse-server:24.8-alpine
